import { styled } from '@mui/material/styles';

const Ul = styled('ul')({
    listStyle: 'none',
    display: 'flex',
    justifyContent: 'space-between',
    padding: 0,
    textAlign: 'center',
});

const Messages = () => (
    <>
        <Ul>
            BBBBB
        </Ul>
    </>
);

export default Messages;
