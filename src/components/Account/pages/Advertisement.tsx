import { Typography } from '@mui/material';
import { styled } from '@mui/material/styles';

const Ul = styled('ul')({
    listStyle: 'none',
    display: 'flex',
    justifyContent: 'space-between',
    padding: 0,
    textAlign: 'center',
});

/* const Li = styled('li')({
    borderBottom: 'solid 1px #000',
    width: '100%',
    paddingBottom: '14px',
    '&:hover': {
        borderBottom: 'solid 3px #000',
        fontWeight: 'bold',

    },
}); */

/* const StyledNavLink = styled(NavLink)({
    textDecoration: 'none',
    color: 'black',
}) */

const Advertisement = () => (
    <>
        <Ul>
        </Ul>
        <Typography variant='h1'>AAAAAAAA</Typography>
    </>

);

export default Advertisement;
